[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    real_provenance: exercise the real credential-provenance function (bypasses the isolation stub)
    integration: marks tests as integration tests (require database)
    e2e: marks tests as end-to-end tests (require all services)
    slow: marks tests as slow running
