# PR-Atlas-Intel-UI-Tailwind-v4-Migration

## Why this slice exists

Dependabot opened `#1783` to bump `atlas-intel-ui` from Tailwind 3.4.19 to
4.3.1. The manifest-only bump could not build because Tailwind v4 moved its
PostCSS plugin out of `tailwindcss` and removed the old
`@tailwind base/components/utilities` CSS entrypoint. Automated review also
flagged the Tailwind v4 CSS entrypoint blocker, and the local grep found the
same legacy `focus:outline-none` utility that Tailwind documents as needing
`focus:outline-hidden` to preserve the old forced-colors behavior.

This exceeds the 400 LOC soft cap because the lockfile churn is generated by
the Tailwind 3 to 4 dependency graph and the required `@tailwindcss/postcss`
adapter. The hand-authored migration is limited to package/config/CSS files and
a mechanical Tailwind utility rename.

## Scope (this PR)

Ownership lane: dependency-maintenance
Slice phase: Production hardening

1. Keep Dependabot's `tailwindcss@4.3.1` upgrade for `atlas-intel-ui`.
2. Add the Tailwind v4 PostCSS adapter package required by the existing Vite
   build path.
3. Migrate the `atlas-intel-ui` CSS entrypoint from removed Tailwind v3
   directives to the Tailwind v4 import.
4. Replace `focus:outline-none` with `focus:outline-hidden` in
   `atlas-intel-ui/src` so high-contrast/forced-colors focus indicators keep
   the old hidden-outline behavior.
5. Leave other UI packages and visual/source refactors out of this slice.

### Review Contract

Acceptance criteria:

- `atlas-intel-ui/postcss.config.js` uses `@tailwindcss/postcss`, not
  `tailwindcss` directly, as the PostCSS plugin.
- `atlas-intel-ui/src/index.css` no longer uses the removed Tailwind v3
  `@tailwind base`, `@tailwind components`, or `@tailwind utilities`
  directives.
- `atlas-intel-ui/src` no longer contains `focus:outline-none`.
- The local `atlas-intel-ui` CI-equivalent path installs cleanly, reports no
  high severity npm audit findings, passes lint, passes the enrolled Node
  tests, builds, and runs the GEO prerender verifiers.
- No package outside `atlas-intel-ui` changes.

Affected surfaces:

- `atlas-intel-ui` package dependency graph.
- `atlas-intel-ui` PostCSS/Tailwind build path.
- `atlas-intel-ui` global CSS entrypoint.
- `atlas-intel-ui` focus-outline utility classes.

Risk areas:

- Tailwind v4 output differences could affect visual styling; this slice proves
  the package build/test path and preserves the documented outline behavior.
- The generated Dependabot body no longer describes the full repair because this
  PR now includes source/config migration files.

Reviewer rules triggered: R2, R8, R9, R12, R14.

### Files touched

- `atlas-intel-ui/package-lock.json`
- `atlas-intel-ui/package.json`
- `atlas-intel-ui/postcss.config.js`
- `atlas-intel-ui/src/components/FilterBar.tsx`
- `atlas-intel-ui/src/components/contentOps/BrandVoiceProfileManager.tsx`
- `atlas-intel-ui/src/index.css`
- `atlas-intel-ui/src/pages/BrandCompare.tsx`
- `atlas-intel-ui/src/pages/ContentOpsAssetsReview.tsx`
- `atlas-intel-ui/src/pages/ContentOpsNewRun.tsx`
- `atlas-intel-ui/src/pages/Login.tsx`
- `atlas-intel-ui/src/pages/Onboarding.tsx`
- `atlas-intel-ui/src/pages/Reviews.tsx`
- `atlas-intel-ui/src/pages/Signup.tsx`
- `atlas-intel-ui/src/pages/b2b/B2BCampaigns.tsx`
- `atlas-intel-ui/src/pages/b2b/B2BOnboarding.tsx`
- `plans/PR-Atlas-Intel-UI-Tailwind-v4-Migration.md`

## Mechanism

Tailwind v4 no longer allows PostCSS to load `tailwindcss` directly as the
plugin. The migration adds `@tailwindcss/postcss@4.3.1` and changes
`postcss.config.js` to load that adapter while leaving `autoprefixer` in place.

Tailwind v4 also replaces the old three-directive CSS entrypoint with a single
`@import "tailwindcss";`. The existing global and prose styles remain after
that import.

The outline migration is mechanical: `focus:outline-none` becomes
`focus:outline-hidden`, matching Tailwind's v4 upgrade guidance for preserving
the old forced-colors behavior.

## Intentional

- The slice does not migrate other Tailwind packages; each Dependabot PR has its
  own package-specific blockers.
- No visual restyling is attempted here. The build path and documented utility
  rename are the required fixes for this dependency/security PR to become
  reviewable.
- `autoprefixer` remains in `postcss.config.js` because it was already part of
  the package pipeline and removing it is not required for the Tailwind v4
  repair.

## Deferred

- Visual preview smoke for `atlas-intel-ui` after CI produces a clean preview
  signal.
- `atlas-mobile` NativeWind/Tailwind migration remains separate because it is
  not safe as a dependency-only Tailwind v4 bump.

Parked hardening: none.

## Verification

- `npm ci && npm audit --audit-level=high && npm run lint && ... && npm run build && npm run verify:blog-geo && npm run verify:landing-page-geo` from
  `atlas-intel-ui`: passed; audit reported `found 0 vulnerabilities`; the
  enrolled Node tests passed; Vite build completed; blog GEO verification
  passed; landing-page GEO verification skipped because no generated
  landing-page sitemap entries were present.
- `rg -n "focus:outline-none|@tailwind base|@tailwind components|@tailwind utilities|tailwindcss:\\s*\\{\\}" atlas-intel-ui -S`: no stale Tailwind v3 CSS
  directives, direct PostCSS `tailwindcss: {}` plugin usage, or
  `focus:outline-none` utilities remained in `atlas-intel-ui`.
- `rg -n "grid-cols-\\[[0-9.]+fr,[0-9.]+fr\\]|grid-cols-\\[minmax\\(0,1fr\\),auto\\]" atlas-intel-ui/src -S`: no stale top-level comma-separated
  arbitrary grid values remained.

## Estimated diff size

| File | LOC |
|---|---:|
| `atlas-intel-ui/package-lock.json` | 1093 |
| `atlas-intel-ui/package.json` | 3 |
| `atlas-intel-ui/postcss.config.js` | 2 |
| `atlas-intel-ui/src/components/FilterBar.tsx` | 4 |
| `atlas-intel-ui/src/components/contentOps/BrandVoiceProfileManager.tsx` | 20 |
| `atlas-intel-ui/src/index.css` | 4 |
| `atlas-intel-ui/src/pages/BrandCompare.tsx` | 2 |
| `atlas-intel-ui/src/pages/ContentOpsAssetsReview.tsx` | 4 |
| `atlas-intel-ui/src/pages/ContentOpsNewRun.tsx` | 44 |
| `atlas-intel-ui/src/pages/Login.tsx` | 4 |
| `atlas-intel-ui/src/pages/Onboarding.tsx` | 2 |
| `atlas-intel-ui/src/pages/Reviews.tsx` | 4 |
| `atlas-intel-ui/src/pages/Signup.tsx` | 8 |
| `atlas-intel-ui/src/pages/b2b/B2BCampaigns.tsx` | 2 |
| `atlas-intel-ui/src/pages/b2b/B2BOnboarding.tsx` | 2 |
| `plans/PR-Atlas-Intel-UI-Tailwind-v4-Migration.md` | 150 |
| **Total** | **1348** |
