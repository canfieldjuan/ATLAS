TBD — paste the news article body here from a desktop session.

Canonical source (per manifest):
  Title:       Noid irked by 'Avoid the Noid' pizza commercials
  Publication: UPI
  Date:        1989-01-31  (day after the standoff — wire-service contemporaneous)
  URL:         https://www.upi.com/Archives/1989/01/31/Noid-irked-by-Avoid-the-Noid-pizza-commercials/7309602226000/

Fallback sources, paste-acceptable order, are documented in
../README.md under "If the canonical UPI source is unavailable".

Automated fetch from this session: UPI returned 403 (bot detection),
WaPo 403 (paywall + bot detection), Snopes 403 (Cloudflare),
web.archive.org tool-blocked. The article body must be pasted
manually from a desktop browser.

When pasting:
  - Strip ads / navigation / "related stories" / cookie banners
  - Preserve paragraph breaks
  - Keep byline + publication + date on the first line
  - If a fallback source is used instead of UPI, prefix the body
    with one header line documenting which source was used. The
    manifest still points at UPI; this header makes the swap
    obvious to downstream stages.

The Stage-1 loader reads this file as `text` on the corresponding
SourceRecord. See ../README.md for selection criteria.
